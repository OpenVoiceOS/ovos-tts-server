name: Pip Audit

on:
  pull_request:
    branches: [dev, master]
  push:
    branches: [dev]
  schedule:
    - cron: "0 6 * * 1"

jobs:
  pip_audit:
    uses: OpenVoiceOS/gh-automations/.github/workflows/pip-audit.yml@dev
    secrets: inherit
